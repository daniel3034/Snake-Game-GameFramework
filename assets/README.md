# assets/

- `eat.wav` — played when snake eats food
- `levelup.wav` — played when player levels up
- `gameover.wav` — played on game over
- `bgm.mp3` or `bgm.ogg` — optional background music

It attempts to load sounds and continues silently if files are missing.
